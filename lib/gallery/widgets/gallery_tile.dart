import 'package:flutter/material.dart';

class GalleryTile extends StatelessWidget {
  const GalleryTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
